<h1>Listas de disciplinas</h1>

<table>
  <tr>
    <th>Nomedisciplina</th>
    <th>Sigla</th>
    <th>Cargahoraria</th>
    <th>Periodo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @disciplinas.each do |disciplina| %>
  <tr>
    <td><%= disciplina.nomedisciplina %></td>
    <td><%= disciplina.sigla %></td>
    <td><%= disciplina.cargahoraria %></td>
    <td><%= disciplina.periodo %></td>
    <td><%= link_to 'Mostrar', disciplina %></td>
    <td><%= link_to 'Editar', edit_disciplina_path(disciplina) %></td>
    <td><%= link_to 'Excluir', disciplina, method: :delete, data: { confirm: 'Você tem certeza ?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Disciplina', new_disciplina_path %>
