<h1>Listas de alunos</h1>

<table>
  <tr>
    <th>Matricula</th>
    <th>Nomealuno</th>
    <th>Sexo</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @alunos.each do |aluno| %>
  <tr>
    <td><%= aluno.matricula %></td>
    <td><%= aluno.nomealuno %></td>
    <td><%= aluno.sexo %></td>
    <td><%= link_to 'Mostrar', aluno %></td>
    <td><%= link_to 'Editar', edit_aluno_path(aluno) %></td>
    <td><%= link_to 'Excluir', aluno, method: :delete, data: { confirm: 'Você tem certeza ?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Aluno', new_aluno_path %>
